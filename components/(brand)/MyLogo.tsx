import { Eye, ScanEye } from "lucide-react";
import React from "react";

const MyLogo = () => {
  return (
    <div className="text-xs cursor-default font-semibold flex items-center gap-2">
      <div>
        <Eye size={18} />
      </div>
      Bounader M.Rafik
    </div>
  );
};

export default MyLogo;
